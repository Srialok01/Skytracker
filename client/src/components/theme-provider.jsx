// A simple theme provider component
const ThemeProvider = ({ children, defaultTheme, storageKey }) => {
  // Since we're simplifying to basic JS, we'll just render children
  return children;
};

export { ThemeProvider };